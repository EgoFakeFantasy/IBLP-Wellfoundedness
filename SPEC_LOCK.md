# 原 IBLP 的形式化边界

唯一主规范为桌面的 `IBLP_wellfoundedness_zh.pdf`（SHA256 `6218983d5e52f4fc8cda17c3376aa813e29150902dcd88a2e2cd0d7cf687bf2c`）及其第2节引用的四页原定义。

- `zero=[]`；六行 `root` 的列、步长和空 marks 必须逐字保持。
- 正文行号一基，Lean 数组位置零基。`fromRight xs k` 表示倒数第 k 项。
- 原 step 是 Nat；正步长、精确普通行型、proper 条件作为可达态不变量证明，不放入新过滤规则。
- trace 包含最终配对源；语义词仅使用去掉末点后的非终因子。
- 原复制保留 `1…n−1`，复制 `p…n`，使用全尾平移。不得复用旧项目 shortCopy 的截尾规则。
- 第一次 raw copy 未定义时回退 cut；之后的合法性须证明，不增加新的回退约定。
- 补全只在 m 次 raw copy 后 cut，再从原行号 n 扫描；新家族跳过。
- 每行 marks 在该行扫描入口冻结。当前检查读当前 trace；新 marks 不进入队列。
- native 沿 q 走阶梯；中行第一次逆删除只删末点、不删中央 e、不减步长。
- 整包新鲜性、分量等宽和有界实现是证明成果，不是附加执行条件。
- 最终关系只由原运行规则定义。目标为 I3 下展开树良基，不自动扩展为键序良序。
- I3 按非平凡初等秩自嵌入定义。最后定理不得添加 RankI2、extender 存在公理或隐藏闭包前提。

FullMarkedBLP 依赖固定于 `832af28724c9897dfaf56248de662aee7b5a9b79`，发布配置已使用公开 Git 地址与精确提交；其他依赖也与完成证明时的版本一致。
